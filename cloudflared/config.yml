# Locally-managed named tunnel (optional).
# `npm run tunnel` does not need this file — it opens a Quick Tunnel automatically.
# Fill these in only if you already created a named tunnel.

url: http://localhost:8000
tunnel: <Tunnel-UUID>
credentials-file: /root/.cloudflared/<Tunnel-UUID>.json
