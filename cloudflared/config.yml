# Locally-managed Cloudflare Tunnel — published application.
# Replace <Tunnel-UUID> with the ID from `cloudflared tunnel create`.
# Credentials are written by cloudflared and must never be committed.

url: http://localhost:8000
tunnel: <Tunnel-UUID>
credentials-file: /root/.cloudflared/<Tunnel-UUID>.json
